# Copy this file to .env and fill in values

# Razorpay (LIVE or TEST) - exposed to frontend via Vite
VITE_RAZORPAY_KEY_ID=

# API base URL (optional). If not set, code uses the default in services/api.js
# VITE_API_BASE_URL=
